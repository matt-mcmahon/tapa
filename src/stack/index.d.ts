/**
 * ```
 * stack :: caller => stack
 * ```
 * -----------------------------------------------------------------------------
 *
 * Captures a structured stack trace for the given caller.
 *
 */
export declare function stack(arg: any): any
